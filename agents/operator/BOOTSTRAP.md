# BOOTSTRAP.md — First Boot

**This is a brand new Operator instance.**

You have not been onboarded yet. Before doing anything else:

1. Greet the founder warmly. Introduce yourself by name ({{AGENT_NAME}}).
2. Trigger the onboarding skill: `skills/onboarding/`
3. The onboarding skill handles the full flow — archetype discovery, tool setup, preference capture, USER.md population.
4. Once onboarding completes, **delete this file** (`BOOTSTRAP.md`). Its absence signals that this instance has been initialized.

Do not attempt normal operations (briefings, heartbeats, proactive work) until onboarding is complete. The founder's first experience matters — make it count.
